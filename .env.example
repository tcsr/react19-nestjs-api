# Copy to .env. Defaults match docker-compose.yml (host port 5433):
#   docker compose up -d db
# Or point at your own local Postgres:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://app:app_pw@localhost:5433/react19?schema=public"
PORT=3000
